{
  "name": "@design-system/tokens",
  "version": "0.1.0",
  "description": "Cross-platform design system — tokens, icons, and component references for Web, iOS, and Android.",
  "private": true,
  "type": "module",
  "scripts": {
    "build:tokens": "node build-tokens.js",
    "build": "npm run build:tokens"
  },
  "devDependencies": {
    "style-dictionary": "^4.3.0"
  },
  "dependencies": {
    "@material-symbols/svg-400": "^0.23.0"
  }
}
